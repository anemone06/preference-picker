# 反派偏好选择器：交给 Agent 的安装与启动说明

## 给接收者

这是一个本地网页小工具，支持 Windows、macOS 和有桌面浏览器的 Linux。无需账号、数据库、Node.js 或 npm。

把 ZIP 和下面这段话发给你的 Agent 即可：

> 请把这个 ZIP 中的反派偏好选择器解压到本机一个长期保留的目录，阅读并执行其中的《给Agent的安装说明.md》。检查 Python 环境，用 start.py 启动本地服务，在我的浏览器里打开页面，并验证两个选项能够正常显示。请保留运行中的服务，最后告诉我项目目录、访问地址、停止和下次启动的方法。不要修改题目或界面，也不要清空已有进度。

“安装”在这里就是解压到固定目录。应用运行不需要网络；若电脑没有 Python，安装 Python 时需要联网。也可以用系统浏览器直接打开解压后的 `index.html`，但浏览器对文件地址的存储限制不同，因此推荐使用下述本地服务。

## Agent 执行步骤

### 1. 解压到长期保留的目录

- 确认操作系统和用户可写的应用或文档目录，把 ZIP 解压为一个独立的 `preference-picker` 目录。
- ZIP 已包含最外层目录，避免再次套一个同名目录。
- 不要放在临时目录或下载软件的临时解压区。
- 如果目标目录已经存在，先检查内容。不要覆盖已有修改；改用新的目录并告诉用户。
- 核对目录中包含 `index.html`、`style.css`、`data.js`、`engine.js`、`export.js`、`app.js` 和 `start.py`。

### 2. 检查 Python

启动器只使用 Python 标准库，推荐 Python 3.9 或更高版本。无需创建虚拟环境或安装任何 pip 包。

Windows 依次检查可用的命令：

```powershell
py -3 --version
python --version
```

macOS / Linux：

```sh
python3 --version
```

选择实际返回 Python 3.9 或更新版本的命令。Windows 的 `python` 可能只是商店入口，不要把入口当成已安装的解释器。

若没有 Python，请遵循当前 Agent 的软件安装权限，通过 Python 官方渠道或操作系统官方包管理器安装受支持的 Python 3，再重新验证版本。如果本机禁止安装，可让用户用系统浏览器打开 `index.html`；若 Agent 自身禁止访问文件地址，不要绕过限制，说明需要用户手动打开。

### 3. 启动

切换到项目目录，并使用上一步确认的解释器。

Windows：

```powershell
py -3 start.py
```

如果只有 `python` 可用，则使用：

```powershell
python start.py
```

macOS / Linux：

```sh
python3 start.py
```

启动器会在 `127.0.0.1:8765` 提供本地服务，并尝试打开默认浏览器。以终端输出的地址为准，通常为：

```text
http://127.0.0.1:8765/
```

项目路径中存在中文或空格不影响启动。也可以使用加引号的 `start.py` 绝对路径运行，服务仍只提供项目目录中的文件。

如果 Agent 的运行环境要求通过专用浏览器工具打开页面，或不允许脚本控制默认浏览器，使用下面的命令启动，再通过允许的浏览器工具打开输出地址：

```sh
python3 start.py --no-browser
```

Windows 相应替换成 `py -3` 或已验证的 `python`。

### 4. 保留服务并验证页面

- 使用能在 Agent 回复后继续运行的终端或持久会话启动服务。不要因为一次命令超时就结束服务器。
- 若默认浏览器没有自动打开，通过当前环境允许的浏览器工具打开终端打印的地址。
- 确认页面标题包含“反派偏好”，能够看到“逐轮选择”“全局视图”和进度。首次使用应显示“认知异常 / 傲慢”，进度为 0 / 46；已有进度时应正常恢复，不要重置。
- 检查 HTML、CSS 和 JavaScript 均成功加载。可以切换全局视图再返回，这不会新增选择；不必替用户完成题目来验收。
- 不要关闭已启动的服务。向用户报告项目绝对路径、实际访问地址、当前服务状态，以及本机适用的启动命令。
- 如果 Agent 运行在远程或无桌面的环境中，要说明服务运行在哪台机器；远程机器的 `127.0.0.1` 不能当作用户电脑的地址。该流程面向用户本机运行。

### 5. 处理常见问题

**端口被占用**

先确认是否已有同一个项目运行。只有确认是本项目时才复用已有地址，不要结束不明进程。否则使用：

```sh
python3 start.py --port 8766
```

以后始终使用选定的端口。不要将服务器绑定到 `0.0.0.0`，无需向外网暴露服务或修改防火墙。

**关闭终端后页面无法打开**

再次运行启动命令即可。启动器不会安装开机自启或系统后台服务。

**进度看起来丢失**

进度存储在浏览器本地，不在 ZIP 或项目文件中。确认使用同一浏览器、同一浏览器用户、相同端口以及相同的 `http://127.0.0.1` 地址。`localhost`、文件直开和其他端口都对应不同的存储位置。复制 ZIP 给别人不会包含原用户的选择。

**页面提示无法保存**

检查浏览器隐私模式或存储限制。页面仍可使用，但不要保证关页后能恢复；不要未经用户同意清除浏览器数据。

## 停止与下次启动

在运行服务器的终端按 `Ctrl+C` 即可停止。关闭网页标签页不会主动停止服务。

下次在同一目录重新运行对应的 `start.py` 命令，保持原来的端口，再打开同一地址。浏览器已保存的进度会恢复。

## 交付边界

此包是跨平台网页源文件和 Python 启动器，不是 Windows EXE 或 macOS App 安装包。应用启动无需第三方依赖。无浏览器、禁止本地脚本或禁止安装 Python 的受管电脑，需要按照其本机限制处理，不能承诺所有环境都能自动安装。
